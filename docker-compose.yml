version: '3.4'
services:
  ####################
  # Backing services #
  ####################
  mongo:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - data:/data/db

  mongo-express:
    image: mongo-express
    ports:
      - 8081:8081

  ################
  # Our services #
  ################

  app:
    build:
      context: .
    image: starter-builder
    ports:
      - "8080:8080"
    depends_on:
      - mongo
    volumes:
      # Mount our local codebase over the codebase baked into the image.
      # Restarting the container will run your local code, no need to rebuild.
      - .:/starter
    command: ["go", "run", "cmd/starter/starter.go", "--debug"]

volumes:
  data:
    driver: local
